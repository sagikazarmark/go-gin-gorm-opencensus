version: "2.1"

services:
    db:
        ports:
            - 3306:3306
        volumes:
            - ./.docker/volumes/mysql:/var/lib/mysql

    prometheus:
        ports:
            - 9090:9090
        volumes:
            - ./.docker/volumes/prometheus:/prometheus
